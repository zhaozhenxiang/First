<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | 默认 Base URL
    |--------------------------------------------------------------------------
    |
    | 所有相对路径请求将基于此 URL。设为空则使用绝对路径。
    |
    */
    'base_url' => env('HTTP_BASE_URL', ''),

    /*
    |--------------------------------------------------------------------------
    | 超时设置（秒）
    |--------------------------------------------------------------------------
    */
    'timeout' => (int) env('HTTP_TIMEOUT', 30),
    'connect_timeout' => (int) env('HTTP_CONNECT_TIMEOUT', 10),

    /*
    |--------------------------------------------------------------------------
    | SSL 验证
    |--------------------------------------------------------------------------
    */
    'verify_ssl' => (bool) env('HTTP_VERIFY_SSL', true),

    /*
    |--------------------------------------------------------------------------
    | 重定向
    |--------------------------------------------------------------------------
    */
    'follow_redirects' => true,
    'max_redirects' => 5,

    /*
    |--------------------------------------------------------------------------
    | 默认请求头
    |--------------------------------------------------------------------------
    */
    'headers' => [
        'User-Agent' => 'First-Framework/1.0',
        'Accept' => 'application/json',
    ],

];
